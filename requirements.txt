# requirements.txt — dependências de produção
# Instalar: pip install -r requirements.txt
# Gerar pins: pip freeze > requirements.txt (a partir de .venv limpo)
#
# Exemplo:
# requests==2.32.3
# python-dotenv==1.0.1
